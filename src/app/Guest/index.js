import GuestLayout from 'app/GuestLayout';
import Login from 'pages/Login';
import React from 'react';

const Guest = ({ routes }) => {
  return <GuestLayout routes={routes} />;
};

export default Guest;
