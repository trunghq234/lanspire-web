import React from 'react';
import MainLayout from 'app/MainLayout';

const Admin = ({ routes, menuItems }) => {
  return <MainLayout routes={routes} menuItems={menuItems} />;
};

export default Admin;
